{
  "name": "beauty-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --host=127.0.0.1 --port=8090 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
